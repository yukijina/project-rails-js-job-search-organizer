<!-- If a logged in user typed login path directly to the url, it shows error msg -->
<p class="text-danger mt-5"><%= flash[:message] %></p>

<% if !@user.checklists.empty? %>
<div class="my-5">
  <h1 class="mb-4">Welcome Back, <%= display_title(@user.username) %> !</h1>
  <p>Your email: <%= @user.email %></p>
  <p><%= "You are logged in as Admin" if admin?(@user) %></p>

  <div class="mt-5">
    <%= link_to "Go to your checklist", user_checklists_path(@user), class: "btn btn-info" %>
    <%= link_to "or Create a new list", new_company_path, class: "btn btn-outline-info" %>
  </div>
</div>

<% else %>
<div class="mt-5">
  <h1 class="mb-4">Welcome, <%= display_title(@user.username) %> !</h1>
  <p>Your email: <%= @user.email %></p>
  <p><%= "You are logged in as Admin" if admin?(@user) %></p>
</div>

<div class="mt-5">
  <h2>How to use Job Search Organizer?</h2>
</div>
<div class="mt-4">
  <h5>First step: </h5>
  <p>Create your own checklist. Add a company and positon you are intersted in.</p>
</div>

<div class="mt-4">
  <h5>Second step:</h5>
  <p>Update your checklist. You can add progress (resume, interview, completed) and a brief note.</p>
</div>

<div class="mt-5">
  <%= link_to "Let's get started", new_company_path, class: "btn btn-info" %>
  <%= link_to "Or check a company who is hiring now", companies_path, class: "btn btn-outline-info" %>
</div>
<% end %>
